// Operator.java - clase o interfaz de utils
package utils.enums;

public enum Operator {
    EQ, // igual
    EN, // no igual
    GT, // mayor que
    LT, // menor que
    GE, // mayor o igual que
    LE // menor o igual que 
}